package ch04.titt2;

// None of the options are correct answer to create an object of the class StringBuilder with a default of 16 characters
public class TwistInTheTale2 {
    public static void main(String[] args) {
        StringBuilder name = StringBuilder.getInstance();
        StringBuilder name = StringBuilder.createInstance();
        StringBuilder name = StringBuilder.buildInstance();
    }
}
